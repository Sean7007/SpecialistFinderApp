<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.specialistfinderapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MAPS_RECEIVE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Genysis.CustomerRegi2"></activity>
        <activity android:name=".SpecialistFragments.StaffActivityHome" />
        <activity android:name=".CustomerFragments.BookingActivity" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_map_key" />

        <activity
            android:name=".CustomerMapActivity"
            android:label="@string/title_activity_customer_map" />
        <activity
            android:name=".Genysis.Customer_Home"
            android:label="@string/title_activity_customer__home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SpecialistFragments.SpecHomeFragment" />
        <activity android:name=".SpecialistFragments.HospitalListActivity" />
        <activity android:name=".CustomerFragments.CustomerHomeFragment" />
        <activity android:name=".CustomerFragments.CardPayment" />
        <activity android:name=".Genysis.SpecialistForgot" />
        <activity android:name=".Genysis.CustomerForgot" />
        <activity android:name=".Genysis.SpecialistHome" />
        <activity android:name=".Genysis.SpecialistHome2" />
        <activity android:name=".Genysis.SpecialistRegi" />
        <activity android:name=".Genysis.SpecialistLogin" />
        <activity android:name=".Genysis.CustomerRegi" />
        <activity android:name=".Genysis.CustomerLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>